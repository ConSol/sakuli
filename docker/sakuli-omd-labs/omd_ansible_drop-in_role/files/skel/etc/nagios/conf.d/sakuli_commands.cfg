define command {
  command_name                   sakuli_eventhandler
  command_line                   $USER2$/sakuli_eventhandler.sh "$SERVICESTATE$" "$HOSTNAME$" "$SERVICEDESC$" "$LASTSERVICECHECK$"
}
